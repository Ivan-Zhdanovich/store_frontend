/**
 * @fileoverview Test the usage of a removed config
 * @author Sencha
 */
'use strict';

// ------------------------------------------------------------------------------
// Requirements
// ------------------------------------------------------------------------------

const rule = require('../../../lib/rules/no-removed-config-usage'),
    RuleTester = require('eslint').RuleTester;

// ------------------------------------------------------------------------------
// Tests
// ------------------------------------------------------------------------------

const ruleTester = new RuleTester({
    settings: {
        extjs: {
            toolkit: 'modern',
            fromVersion: '6.0',
            toVersion: '7.9.0'
        }
    }
});
ruleTester.run('no-removed-config-usage', rule, {

    valid: [
        
    ],

    invalid: [
        {
            code: `
                Ext.define('Oreilly.view.Location', {
                    extend: 'Ext.Container',
                    requires: 'Ext.BingMap',
                    initialize: function () {
                        var map;

                        this.callParent();

                        map = this.add({
                            xtype: 'bingmap',
                            mapOptions: {
                                center: position,
                                mapTypeId: Microsoft.Maps.MapTypeId.ROADMAP,
                                callback: mapCallback
                            }
                        });
                    }
                });
            `,
            errors: [
                { message: /Usage of removed config 'xtype' found for 'Ext.BingMap'/ },
                { message: /Usage of removed config 'mapOptions' found for 'Ext.BingMap'/ }
            ]
        }
    ]
});
